{
  "name": "@auro-formkit/typescript",
  "description": "Shared Typescript configurations for auro-formkit monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "@types/mocha": "^10.0.9",
    "@types/chai": "^5.0.0"
  },
  "engines": {
    "node": "^20.x || ^22.x"
  }
}