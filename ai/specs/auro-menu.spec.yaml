# GENERATED by packages/build-tools/src/aiSpecGenerator.mjs — do not edit by hand.
# Authored inputs live in ai/_authored/auro-menu.overlay.yaml
component: auro-menu
version: 0.0.0
summary: Selectable option list; native closest is a <ul role="listbox"> with role="option" items, or the <option>s inside a native <select>.
generatedFrom:
  cem: custom-elements.json#auro-menu
  cssRecipe: components/menu/demo/css-only.md
  overlay: ai/_authored/auro-menu.overlay.yaml
sourceOfTruth:
  component: components/menu/src/auro-menu.js
visual:
  tokensImport: ./node_modules/@aurodesignsystem/design-tokens/dist/tokens/CSSCustomProperties.css
  rule: NEVER hardcode a hex, px, rem, or font-family value. Every visual value MUST be an Auro --ds-* token via var(--ds-token, fallback). Literal values are allowed only as the second argument (fallback) of var().
  css: |-
    @import "./node_modules/@aurodesignsystem/design-tokens/dist/tokens/CSSCustomProperties.css";

    .menu {
      list-style: none;
      margin: 0;
      padding: var(--ds-size-100, 0.5rem) 0;
      background-color: var(--ds-color-container-primary-default, #ffffff);
      border: 1px solid var(--ds-color-border-ui-default, #6e767f);
      border-radius: var(--ds-border-radius, 4px);
    }

    .menu__option {
      padding: var(--ds-size-100, 0.5rem) var(--ds-size-200, 1rem);
      color: var(--ds-color-text-primary-default, #1d1f24);
      cursor: pointer;
      user-select: none;
    }

    .menu__option:hover {
      background-color: var(--ds-color-container-ui-hover-default, #f0f5ff);
    }

    .menu__option:focus-visible {
      outline: 2px solid var(--ds-color-border-active-default, #01426a);
      outline-offset: -2px;
    }

    .menu__option[aria-selected="true"] {
      background-color: var(--ds-color-container-ui-selected-default, #01426a);
      color: var(--ds-color-text-inverse-default, #ffffff);
    }

    .menu__option[aria-disabled="true"] {
      cursor: not-allowed;
      opacity: 0.5;
    }
api:
  attributes:
    - name: disabled
      tier: core
      native: disabled
      description: When true, the entire menu and all options are disabled.
    - name: loading
      tier: wc-only
      default: 'false'
      description: When true, displays a loading state using the loadingIcon and loadingText slots if provided.
    - name: matchword
      tier: wc-only
      default: undefined
      description: Specifies a string used to highlight matched string parts in options.
    - name: multiselect
      tier: core
      native: aria-multiselectable="true" on the listbox (or the native `multiple` attribute)
      default: 'false'
      description: When true, the selected option can be multiple options.
    - name: nocheckmark
      tier: wc-only
      default: 'false'
      description: When true, selected option will not show the checkmark.
    - name: value
      tier: core
      native: value
      default: undefined
      description: The value of the selected option.
    - name: layout
      tier: wc-only
      default: '{''default''}'
      description: Defines the language of an element.
    - name: shape
      tier: wc-only
    - name: size
      tier: enhanced
    - name: ondark
      tier: wc-only
  slots:
    - name: loadingText
      tier: core
      description: Text to show while loading attribute is set
    - name: loadingIcon
      tier: core
      description: Icon to show while loading attribute is set
    - name: (default)
      tier: core
      description: Slot for insertion of menu options.
  events:
    - name: auroMenu-optionsChange
      tier: wc-only
    - name: auroMenu-activatedOption
      tier: wc-only
      description: Notifies that a menuoption has been made `active`.
    - name: auroMenu-customEventFired
      tier: wc-only
      description: Notifies that a custom event has been fired.
    - name: auroMenu-loadingChange
      tier: wc-only
      description: Notifies when the loading attribute is changed.
    - name: auroMenu-selectValueFailure
      tier: wc-only
      description: Notifies that an attempt to select a menuoption by matching a value has failed.
    - name: auroMenu-selectValueReset
      tier: wc-only
      description: Notifies that the component value has been reset.
    - name: auroMenu-selectedOption
      tier: wc-only
      description: Notifies that a new menuoption selection has been made.
  methods:
    - name: register
      tier: wc-only
    - name: selectByValue
      tier: wc-only
    - name: reset
      tier: wc-only
    - name: navigateOptions
      tier: wc-only
    - name: updateActiveOption
      tier: wc-only
    - name: renderLayout
      tier: wc-only
    - name: resetShapeClasses
      tier: wc-only
    - name: resetLayoutClasses
      tier: wc-only
    - name: updateComponentArchitecture
      tier: wc-only
behavior:
  a11y:
    - Every control MUST have a programmatically associated <label> (for/id or wrapping).
    - Link help text via aria-describedby so it is announced after the label.
    - Reflect invalid state with aria-invalid="true|false" on the control.
    - Render validation errors with role="alert" aria-live="assertive" so they announce immediately.
    - Use role="listbox" with role="option" children; set aria-selected on chosen options and aria-activedescendant for the highlighted one.
  keyboard:
    - All interactive affordances are real, focusable elements in DOM order.
    - Up/Down move the active option, Enter/Space selects, type-ahead jumps to matching options, Home/End jump to the ends.
  validation: []
gaps:
  - id: active-option-tracking
    feature: Active-option tracking
    guidance: auro-menu tracks the active option internally (via an active CSS class on the current auro-menuoption) rather than moving DOM focus between options.
  - id: arrow-key-navigation
    feature: Arrow-key navigation
    guidance: auro-menu handles ArrowUp, ArrowDown, Enter, and Tab to move the highlight and commit selections.
  - id: matchword-highlighting
    feature: matchWord highlighting
    guidance: auro-menu supports a matchWord attribute that highlights matched substrings within option labels — used by auro-combobox to visually mark the user's typed input inside each filtered option.
  - id: type-ahead-navigation
    feature: Type-ahead navigation
    guidance: Type-ahead — jumping the active option to whichever entry starts with the letters the user typed — is not handled by auro-menu itself.
  - id: nested-submenu-support
    feature: Nested submenu support
    guidance: auro-menu detects nested auro-menu elements, applies the correct role="group", and computes indentation per level.
  - id: selection-state-coordination
    feature: Selection state coordination
    guidance: auro-menu keeps optionSelected, value, and each option's aria-selected state in sync internally.
  - id: aria-roles-and-live-announcements
    feature: ARIA roles and live announcements
    guidance: auro-menu automatically applies role="listbox", aria-multiselectable, aria-busy during loading, and per-option role="option" with aria-selected and aria-disabled.
  - id: value-emission-and-events
    feature: Value emission and events
    guidance: auro-menu dispatches a structured set of events — auroMenu-selectedOption, auroMenu-activatedOption, auroMenu-optionsChange, auroMenu-selectValueReset, auroMenu-selectValueFailure, and auroMenu-loadingChange — so parent components can react to highlight, selection, and lifecycle changes.
  - id: multi-select-with-array-value
    feature: Multi-select with array value
    guidance: auro-menu supports multiselect, exposing the selection as a JSON-stringified array via value and as an array of elements via optionSelected.
  - id: multi-select-deselect
    feature: Multi-select deselect
    guidance: In multi-select mode, clicking an already-selected option toggles it off.
  - id: select-by-value-and-reset
    feature: Select-by-value and reset
    guidance: auro-menu exposes a value attribute that drives selection programmatically (accepting a JSON-stringified array in multi-select mode) and a single reset() method that clears all selection and validation state.
  - id: loading-state
    feature: Loading state
    guidance: auro-menu renders a built-in loading placeholder via loadingIcon and loadingText slots, sets aria-busy, and emits auroMenu-loadingChange when the loading attribute toggles.
  - id: shape-and-size-variants
    feature: Shape and size variants
    guidance: auro-menu supports shape (box, round) and size (sm, md) variants that adjust spacing and corner radii consistently across the menu and its options.
  - id: multi-brand-theming
    feature: Multi-brand theming
    guidance: auro-menu consumes Auro design tokens through its component logic, so it automatically picks up brand-specific theming when token packages are swapped.
examples:
  - id: basic-listbox
    html: |-
      <ul class="menu" role="listbox" aria-label="Cabin class">
        <li class="menu__option" role="option" tabindex="0" aria-selected="true">Economy</li>
        <li class="menu__option" role="option" tabindex="-1" aria-selected="false">Premium</li>
        <li class="menu__option" role="option" tabindex="-1" aria-selected="false">First</li>
      </ul>
  - id: listbox-with-a-disabled-option
    html: |-
      <ul class="menu" role="listbox" aria-label="Seat">
        <li class="menu__option" role="option" tabindex="0" aria-selected="false">12A</li>
        <li class="menu__option" role="option" tabindex="-1" aria-selected="false" aria-disabled="true">12B</li>
        <li class="menu__option" role="option" tabindex="-1" aria-selected="false">12C</li>
      </ul>
verify:
  - every color, size, spacing, radius, and font value uses var(--ds-*)
  - each control has an associated <label>
  - controls with aria-invalid="true" have a role="alert" message referenced by aria-describedby
  - 'axe: label, aria-valid-attr-value, color-contrast pass'
